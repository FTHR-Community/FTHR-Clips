# Pinned runtime dependencies for release builds, maintained from requirements.in.
# Install with: python -m pip install -r requirements-alpha.txt
# Target: CPython 3.14. Recheck clean installs after changing any pin.

# Direct dependencies
PySide6==6.11.1
opencv-python-headless==4.13.0.92
numpy==2.4.4
sounddevice==0.5.5
jeepney==0.9.0; sys_platform != "win32"

# Transitive dependencies — pinned so a silent upstream release cannot change
# what an alpha build contains.
PySide6-Addons==6.11.1    # via PySide6 (Qt Multimedia)
PySide6-Essentials==6.11.1 # via PySide6
shiboken6==6.11.1         # via PySide6
cffi==2.0.0               # via sounddevice
pycparser==3.0            # via cffi
